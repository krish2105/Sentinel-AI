# Backend API base URL. In local dev the Next.js rewrite proxies /api -> this.
# Local:
NEXT_PUBLIC_API_URL=http://localhost:8000
# Production (Render backend):
# NEXT_PUBLIC_API_URL=https://sentinel-backend.onrender.com
